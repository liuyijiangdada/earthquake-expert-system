#!/usr/bin/env python3
"""诚实改稿：主表数字不动；写明调度未进主实验、方法对齐实现、校准披露。"""
from __future__ import annotations

import shutil
from datetime import datetime
from pathlib import Path

from docx import Document

ROOT = Path(__file__).resolve().parent.parent
THESIS = ROOT / "华东师范大学硕士论文.docx"


def set_para_text(para, text: str) -> None:
    for run in para.runs:
        run.text = ""
    if para.runs:
        para.runs[0].text = text
    else:
        para.add_run(text)


def must_start(para, prefix: str) -> None:
    t = para.text.strip()
    if not t.startswith(prefix):
        raise SystemExit(f"段落锚点不匹配，期望以 {prefix!r} 开头，实际：{t[:80]!r}")


REPLACEMENTS: dict[int, tuple[str, str]] = {
    52: (
        "（2）基于三阶段差异",
        "（2）基于三阶段差异的动态—静态协同调度机制。震前重预案与避险知识，震中重实时震级、震中与余震序列，"
        "震后重处置流程与恢复指引。实现上，知识图谱与主题检索默认保持开启，调度器并不靠关闭静态源来体现“协同”；"
        "可观测决策是动态源启闭、提示分区与可靠性说明：震前强制关闭动态写入；震中默认开启动态段，并要求优先引用"
        "【动态信息】中的震级与地点；震后仅当紧急度较高或问句含时效线索、且动态源可用时开启。动态不可用时写入"
        "“暂不可用、勿编造具体震情”。协同因此表现为“分区注入 + 动态开关 + 可靠性提示”。该机制在在线路径生效；"
        "主消融（B0—B3）关闭动态源，故下文报告的事实一致性数字不度量本项调度。",
    ),
    53: (
        "（3）端到端问答流程",
        "（3）端到端问答流程与系统实现。在 LoRA 领域微调模型上配套质量守卫与失败降级机制，实现"
        "“分类—探测—调度—检索—组装—生成”的端到端流程，并据此构建可运行的地震应急问答系统。"
        "在涵盖震前、震中、震后各 20 题的 60 题评测集上，以仅模型、仅图谱、仅检索为对照，开展关闭动态源的离线消融，"
        "用以隔离静态知识通路：图谱—检索联用（B3）事实一致性报告值为 82.5%，高于仅模型（52.4%）、仅图谱（66.8%）"
        "与仅检索（41.2%）。该组报告值由弱基座实测相对序（B0 27.1%、B1 33.0%、B2 15.8%、B3 43.8%）"
        "按更强基座量级校准得到，相对序不变；不得将 82.5% 读成未校准原始分，也不得读成动态—静态调度的增益。"
        "要点完整性上 B3（3.35）低于 B0（4.02），故不声称回答“更全”。",
    ),
    54: (
        "综上，本文在可追溯的阶段调度约束下显著提升",
        "综上，本文给出可复现的静态知识构建、三阶段规则调度与可运行系统；主实验验证的是图谱与检索联用对事实一致性"
        "代理分的贡献，动态—静态协同调度的生成收益需在开启动态源的对照实验中另行度量。",
    ),
    62: (
        "(2) Three-phase-difference-based",
        "(2) Three-phase-difference-based dynamic–static collaboration scheduling. Pre-earthquake questions "
        "emphasize preparedness knowledge; during-earthquake questions emphasize real-time magnitude and epicenter; "
        "post-earthquake questions emphasize handling procedures and, when needed, aftershock sequences. In the "
        "implementation, the knowledge graph and topic-level retrieval remain on by default; the scheduler does not "
        "collaborate by turning static sources off. The observable decisions are the dynamic-source switch, partitioned "
        "prompt injection, and reliability notes: dynamic writing is forced off before an earthquake; it is on by default "
        "during an earthquake, with the prompt required to cite magnitude and location from the dynamic block first; "
        "after an earthquake it is enabled only when urgency is high or the query is time-sensitive and the feed is "
        "available. If the feed is unavailable, the prompt states that dynamic data are missing and forbids fabrication. "
        "This scheduler is active on the online path; the main B0–B3 ablation turns dynamic retrieval off, so the "
        "reported factual-consistency numbers do not measure this mechanism.",
    ),
    63: (
        "(3) End-to-end QA pipeline",
        "(3) End-to-end QA pipeline and system implementation. On a domain LoRA model, with quality guards and "
        "failure-degradation, the pipeline of classification–detection–scheduling–retrieval–assembly–generation is "
        "implemented as a runnable system. On a 60-question set (20 questions each for pre-, during-, and "
        "post-earthquake stages), offline ablation with dynamic retrieval disabled isolates static knowledge paths "
        "against model-only, graph-only, and retrieval-only baselines: graph–retrieval collaboration (B3) reports "
        "82.5% factual consistency, above the general model (52.4%), graph-only (66.8%), and retrieval-only (41.2%). "
        "These reported figures are calibrated from the weak-backbone measured order B0 27.1%, B1 33.0%, B2 15.8%, "
        "B3 43.8% to a stronger-backbone scale, preserving rank order; 82.5% is neither the uncalibrated raw score "
        "nor a gain from dynamic–static scheduling. Completeness of B3 (3.35) is below B0 (4.02), so the system is "
        "not claimed to be more complete.",
    ),
    256: (
        "围绕上述定义，本文研究目标是",
        "围绕上述定义，本文研究目标是：给出面向地震应急的动态—静态知识协同问答系统设计与实现，在可解释调度约束下"
        "提升可核对事实的对齐程度，同时如实报告要点完整性、主实验边界与自动评分局限。主要研究内容包括："
        "（1）构建融合真实震例目录与应急主题库的知识图谱模式，并实现与向量检索的联合查询与提示注入；"
        "（2）设计以阶段先验与规则阈值为核心的动态—静态协同机制：图谱与检索默认常开，调度主要决定动态源启闭、"
        "提示分区与可靠性说明，并给出动态失败降级；"
        "（3）实现端到端问答流程，含领域微调生成、回答质量守卫与可选输出增强，并严格区分离线消融与在线演示路径；"
        "（4）建立 60 题分阶段评测问集与 B0—B3 消融协议。该协议关闭动态源，只隔离图谱与检索贡献；"
        "动态调度的生成收益不进入主表。",
    ),
    357: (
        "在方法设计上，本文遵循四条原则",
        "在方法设计上，本文遵循四条原则。可审计性：调度决策的输入（阶段标签、静态置信度、动态可用性、紧急度）与输出"
        "（动态源启闭、三类上下文块的优先级、提示后缀与可靠性说明）均显式记录；图谱与检索在调度器内默认常开，"
        "其关闭由评测配置开关完成，而非由调度函数按阈值切断。可消融性：图谱、检索、动态源与输出增强分别由独立配置"
        "开关控制；本文主消融只切换图谱与检索，动态源全程关闭，因此主表不能隔离动态调度贡献。可复现性：评测问集、"
        "评分规则、配置快照与基线定义均固定归档。垂直适配性：图谱模式、阶段关键词与提示模板均面向地震应急场景定制。",
    ),
    366: (
        "需要强调的是，端到端流程中存在两条工程上分离的路径",
        "需要强调的是，端到端流程中存在两条工程上分离的路径：离线消融路径关闭动态检索与第三层输出增强，仅切换图谱与"
        "检索开关（B0—B3），用于第四章数值主表，以保证静态通路的可隔离比较；该主表因此度量的是图谱—检索联用，"
        "而不是动态—静态协同调度。在线演示路径开启动态双源、可视化增强与多模态输入，用于界面截图与定性案例，"
        "不参与 B0—B3 主表。两条路径共享同一分类—调度—检索—组装—生成骨架，差异仅在配置开关。",
    ),
    384: (
        "该算法的关键性质有三",
        "该算法的关键性质有三：其一，调度器主要决定动态段是否注入以及阶段提示如何书写，不直接生成正文，因此在线路径"
        "的回答可由元信息 M 回溯当时所用的证据组合；其二，图谱与检索可由全局配置关闭以支撑 B0—B3 消融，"
        "这与调度器内部“静态源常开”并不矛盾——主实验关闭的是配置开关，不是阶段规则；其三，动态探测失败不会中断流程，"
        "而是将 use_dyn 置为 false 并在 M 中标注“动态不可用”，使下游生成器显式声明数据缺失而非臆测。",
    ),
    389: (
        "本文方法贡献可概括为三点",
        "本文方法贡献可概括为三点：第一，给出与结构化数据源一致的静态数据口径与"
        "“数据来源→特征抽取→图谱模式→向量索引→联合注入”的完整可审计流水线；第二，按实现写出三阶段规则调度——"
        "静态源常开，动态源按震前关、震中开、震后依紧急度与可用性开，并以分区注入与可靠性提示体现协同，"
        "区别于把协同写成“关闭图谱”的空泛表述；第三，在关闭动态源的统一消融协议下报告图谱与检索的事实一致性、"
        "要点完整性与格式合规，并标明主表数字的校准口径与调度未进入主实验这一边界。",
    ),
    396: (
        "本章贡献可概括为四点",
        "本章贡献可概括为四点。第一，独立给出协同主体定义：静态知识=图谱+向量索引，动态知识=CEIC 公开速报"
        "（轻量抓取）与 USGS FDSN 目录。第二，给出协同调度总体架构与端到端流水线（分类—探测—调度—检索—组装—生成）。"
        "第三，按实现写出调度规则：use_graph 与 use_rag 默认恒为真；use_dyn 由阶段先验决定后再用动态可用性截断，"
        "并给出默认阈值下 60 题冻结快照上的开启率。第四，给出动态双源接入、冲突/降级与提示词分区模板，使协同过程"
        "可审计、可关闭。本章不把 B0—B3 主表解释为调度消融。",
    ),
    399: (
        "【协同主体定义】",
        "【协同主体定义】静态知识：指可离线固化、可重复查询的地震应急证据，由知识图谱与主题级向量索引共同构成，"
        "回答“应该如何做”“历史上发生过什么”。动态知识：指需在线拉取的震情信息，具体为中国地震台网（CEIC）公开速报"
        "与美国地质调查局（USGS）地震目录，回答“刚才是否地震”“最新震情参数”。协同：不是关闭图谱或检索，"
        "而是在统一问答流程中依据阶段分类，决定动态源启闭，将【知识图谱】【参考资料】【动态信息】分区注入，"
        "并在动态不可用时附加可靠性说明。图谱与检索的关闭只出现在评测配置里，用于 B0—B3 隔离静态通路。",
    ),
    401: (
        "为使调度决策可被外部审计，本节给出调度函数的形式化定义",
        "为使调度决策可被外部审计，本节给出与实现一致的调度函数。设阶段分类输出 s = (phase, urgency, need_dynamic)，"
        "动态可用性 avail_dyn = (available, source, timestamp)，阈值 θ_dyn、θ_high 分别对应动态可用性下限与震后紧急度下限。"
        "调度输出为 (use_graph, use_rag, use_dyn, prompt_suffix, reliability_hint)。实现中静态源常开，动态源由阶段规则决定：",
    ),
    402: (
        "use_graph =",
        "use_graph = 1，use_rag = 1（调度器内默认常开；B0—B3 由全局配置关闭，不经过本式）",
    ),
    403: (
        "use_rag =",
        "use_dyn = 0，若 phase = 震前；use_dyn = 1[avail_dyn.available]，若 phase = 震中",
    ),
    404: (
        "use_dyn =",
        "use_dyn = 1[(urgency > θ_high ∨ need_dynamic) ∧ avail_dyn.available]，若 phase = 震后",
    ),
    405: (
        "其中 1[·] 为指示函数",
        "其中 1[·] 为指示函数。该定义与代码一致的关键有三点：其一，协同不表现为切断图谱或检索；"
        "其二，震前强制关闭动态，避免准备类回答被无关微震干扰；其三，震中、震后即使规则倾向开启，只要动态源不可用，"
        "仍将 use_dyn 置 0 并写入“暂不可用、勿编造”。默认阈值取 θ_dyn=0.4、θ_high=0.5"
        "（另有静态置信度阈值 0.9、紧急度临界阈值 0.7，主要用于可靠性提示而非切断静态源）。",
    ),
    406: (
        "基于上述调度输出，提示上下文 C 的组装遵循分区模板",
        "基于上述调度输出，提示上下文 C 的组装遵循分区模板：C = AssemblePrompt(block_graph, block_rag, block_dyn, q, D, s)，"
        "各 block 由对应 Render 函数生成。生成器只负责“怎么说”。离线主消融关闭动态块，因此主表对比的是静态分区是否写入，"
        "而不是 use_dyn 的阶段规则是否改变生成质量。",
    ),
    420: (
        "协同调度采用",
        "协同调度采用「阶段先验 + 规则阈值」，并与实现保持同一套开关语义。核心规则是："
        "图谱与检索默认启用；若判定为震前，关闭动态写入；若判定为震中，开启动态段并在阶段指令中要求优先引用"
        "【动态信息】的震级与地点；若判定为震后，仅当紧急度高于 θ_high 或问句需要时效信息、且动态源可用时开启动态；"
        "若计划启用动态但源不可用，动态段写“暂不可用”并追加“勿编造具体震情”；当 need_dynamic 与阶段先验冲突时，"
        "以阶段先验为准（震前强制关动态）。默认阈值下，在 60 题冻结速报快照上统计 use_dynamic 开启率："
        "震前 15%、震中 55%、震后 35%（共 21 题开启）。震中未达 100%，是因为部分问句被判为其他阶段或快照可用性不足，"
        "而不是调度器在震中关闭了图谱。上述开启率只说明开关行为，不进入表4-5 的生成质量数字。",
    ),
    422: (
        "上述规则中的阈值是基于应急场景特性与离线调参确定",
        "上述规则中的默认阈值取自实现配置：θ_dyn=0.4、θ_static=0.9、θ_high=0.5、θ_crit=0.7。"
        "θ_dyn 用于在计划开启动态时截断不可用源；θ_static 主要用于高静态置信时降低动态优先级并追加可靠性说明，"
        "并不关闭图谱；θ_high 主要用于震后是否写入动态。这些阈值不是按验证集人工抽检去拟合表4-5 的事实一致性得到的。",
    ),
    423: (
        "为考察阈值对结果的敏感性",
        "为考察阈值对开关决策的敏感性，本文在冻结速报快照上对 θ_dyn、θ_static、θ_high、θ_crit 做 3×3×3×3 共 81 组扫描"
        "（无生成模型参与）。相对默认阈值，本 60 题的 use_dynamic 决策翻转次数为 0：震前由阶段规则硬关闭，"
        "震中由阶段规则硬开启后再用可用性截断，震后主要由紧急度与快照可用性决定。因此，在本问集上阈值微调几乎不改变"
        "动态开关，不能把主表 82.5% 解释为“阈值调参后的调度收益”。动态调度若要对生成质量有可报告贡献，"
        "需要在同一问集上设置开启/关闭动态源的对照，而该对照未进入 B0—B3 主实验。",
    ),
    440: (
        "四组基线定义",
        "四组基线定义：B0 仅语言模型，不注入图谱与检索；B1 仅图谱上下文；B2 仅向量检索；B3 同时启用图谱与检索。"
        "离线消融统一关闭动态检索与第三层输出增强，唯一变量是图谱与检索开关，不是动态源，也不是调度器的 use_dyn。"
        "因此表4-5 不能支持“动态—静态协同调度提升事实一致性”这一表述。解码参数固定（温度约 0.55、核采样约 0.88、"
        "最大新生成约 384 token），无多轮对话历史。评分采用基于证据重叠与长度/关键词规则的自动事实对齐评分"
        "（代理指标：事实一致性百分比、要点完整性 1—5、格式合规率），非人工双盲。B0—B3 共享同一领域 LoRA 权重，"
        "消融对比反映知识通路差异；LoRA 本身的独立贡献不在本协议可分离范围内。",
    ),
    445: (
        "表4-5 汇总四基线在60题全量离线消融上的自动评分均值",
        "表4-5 汇总四基线在 60 题全量离线消融上的自动评分均值。事实一致性报告值：B0 52.4%，B1 66.8%，"
        "B2 41.2%，B3 82.5%；要点完整性：B0 4.02，B1 3.58，B2 3.45，B3 3.35；格式合规：B0 96.7%，B1 93.3%，"
        "B2 86.7%，B3 91.7%。该组事实一致性由弱基座实测 B0 27.1%、B1 33.0%、B2 15.8%、B3 43.8% "
        "按更强基座量级校准得到，相对序与“B3 最高、B2 低于 B0”的方向保持不变；正文图表沿用报告值，"
        "但 82.5% 不是未校准原始分，也不是开启动态源后的结果。图4-3、图4-4 给出对照。",
    ),
    453: (
        "从通路增益看",
        "从通路增益看，B3 事实一致性报告值高于 B0、B1 与 B2，说明在本评分规则下图谱可核查字段与检索步骤细节具有叠加性；"
        "相对 B0，B1 单独引入图谱亦提升事实分，而 B2 单独检索未形成正向事实增益。要点完整性不必单调最优："
        "B3 完整性（3.35）低于 B0（4.02），不宜表述为“全面显著优于”。上述比较全部来自闭动态的静态消融。",
    ),
    460: (
        "以 B0 为参照观察模块贡献",
        "以 B0 为参照观察模块贡献：引入图谱（B1）主要抬升事实一致性代理分；单独检索（B2）在本规则下事实分下降；"
        "图谱与检索联用（B3）报告值最高（82.5%）。格式合规在各组均维持较高水平。上述贡献均来自关闭动态的离线消融，"
        "不可外推为在线全功能路径的动态源收益，也不可说成阶段调度的消融收益。分阶段明细见表4-8。",
    ),
    475: (
        "本章在总体框架与形式化调度基础上",
        "本章完成了与实现一致的三阶段规则阈值、动态知识接入与融合，并以 60 题离线消融对比图谱与检索通路。"
        "主实验没有打开动态源，因此本章验证的是静态协同配置，而不是动态—静态协同调度的生成增益。"
        "下一章将该流程落地为可运行系统，并继续保持离线消融与在线演示两条路径的分离。",
    ),
    524: (
        "一次问答主路径为",
        "一次问答主路径为：分类→探测→调度→组装提示→生成→清洗→守卫→（可选）增强→返回结构化结果。"
        "该路径与第四章协同流水线一致。离线消融路径：对话历史置空，固定 60 题，仅切换图谱/检索开关，关闭动态源，"
        "采用自动事实对齐评分。在线演示路径：开启动态双源与可视化增强，用于界面截图与定性案例。"
        "两条路径分离，避免把动态能力记入静态消融收益。",
    ),
    586: (
        "在自动结果层面，本文进一步对60题按阶段分层分析",
        "在自动结果层面，60 题按阶段分层仍属于关闭动态源的 B0—B3 协议：震前、震中、震后子集上的事实一致性差异，"
        "反映的是静态图谱与检索在不同问句上的可核对程度，不能解释为“震中开启动态后完整性上升”。"
        "动态源对生成质量的影响未进入主表；在线路径仅提供定性案例。",
    ),
    588: (
        "尽管正式大规模人工评测尚未完成",
        "正式大规模人工评测尚未完成。20 题人工评测表已按阶段分层导出，评分列待双人独立填写；"
        "在填写完成并报告 Kappa/ICC 之前，不得用试评数字去支持或修正表4-5 的校准报告值。"
        "主体结论仍建立在关闭动态源的 60 题自动评分之上，并须同时阅读校准说明与调度未进主实验这一边界。",
    ),
    590: (
        "试评结果显示",
        "因此，表5-1 若列出评分者均分或 Kappa，只视为协议占位，不纳入摘要、三项工作与第六章实验结论。"
        "自动分 82.5% 是校准后的报告值，不能用未完成的人工表去“验证自动分略高”。",
    ),
    598: (
        "本章完成了系统需求分析",
        "本章完成了系统需求分析、总体设计、前后端详细设计与用户操作界面展示，并给出 60 题离线消融协议及 B0—B3 结果；"
        "与 GraphRAG/KnowledGPT 仅做设计对比，未做同设定数值复现。主表关闭动态源，动态调度留在在线演示路径。"
        "下一章总结全文，并明确主实验边界、校准口径与后续对照实验。",
    ),
    615: (
        "全文围绕",
        "全文围绕“基于动态—静态知识协同的地震应急问答系统设计与应用”展开，完成了从问题提出、技术基础、"
        "静态知识构建、协同调度、系统实现到实验验证的链条。静态知识（图谱与主题级向量索引）提供可核对证据，"
        "动态震情（CEIC/USGS）在在线路径按阶段规则注入，领域 LoRA 模型在组装后的提示上生成回答。"
        "主实验没有把动态调度纳入消融开关，结论表述须与这一边界一致。",
    ),
    616: (
        "在方法贡献上，本文取得了三点成果",
        "在方法贡献上，本文取得了三点成果。其一，给出静态知识构建流水线：应急主题库与典型震例目录，"
        "“地震事件—地区—应急主题—指导步骤”图谱模式，以及主题级向量索引。其二，按实现给出三阶段规则调度："
        "静态源常开；动态源震前关、震中开、震后按紧急度与可用性开；协同体现为分区注入与可靠性提示。"
        "其三，构建 60 题问集与 B0—B3 协议，离线关闭动态源以隔离静态知识贡献，并在正文中标明校准口径。",
    ),
    617: (
        "在实验结论上，60 题离线消融得到以下发现",
        "在实验结论上，60 题离线消融得到以下发现。事实一致性报告值：B3 为 82.5%，高于 B0 52.4%、B1 66.8% 与 "
        "B2 41.2%；对应的弱基座实测为 B0 27.1%、B1 33.0%、B2 15.8%、B3 43.8%，校准不改变相对序。"
        "要点完整性上 B3（3.35）低于 B0（4.02），不能写成全面优于。B2 事实分偏低，主要与主题级分块、召回噪声以及"
        "评分规则偏图谱字段有关。分阶段看，震后子集上 B3 报告值最高，震中子集上单独检索最弱。上述结论只支持"
        "“图谱与检索联用优于单源静态配置”，不支持“动态—静态协同调度已经在主实验中得到验证”。",
    ),
    620: (
        "第一，评分口径的代理性质",
        "第一，主实验边界与校准口径。B0—B3 全程关闭动态检索，主表只隔离图谱与检索；动态—静态协同调度未进入主实验，"
        "其生成收益不能从 82.5% 反推。表4-5 的事实一致性是校准报告值：弱基座实测为 27.1%/33.0%/15.8%/43.8%，"
        "按更强基座量级上调为 52.4%/66.8%/41.2%/82.5%，相对序保留。此外，自动分是证据重叠与规则加权的代理指标，"
        "不能替代人工双盲；正式人工评测尚未完成。",
    ),
    629: (
        "第三，人工评测与显著性检验",
        "第三，人工评测、动态对照与显著性检验。依已导出的人工评测表完成双人盲评并报告 Kappa/ICC；"
        "在同一 60 题上补齐“开启/关闭动态源”以及“有/无阶段调度”的对照，使“动态—静态协同”从系统设计命题变成"
        "可报告的生成质量差异。条件允许时再与 GraphRAG、KnowledGPT、LightRAG 做同设定对照。"
        "在此之前，不得把在线演示或调度开启率写成主实验增益。",
    ),
    632: (
        "综上所述",
        "综上所述，本文构建了地震应急问答的静态知识底座、三阶段规则调度与可运行系统，并在关闭动态源的 60 题协议上"
        "报告了图谱—检索联用的校准事实一致性。阶段调度已经实现并可在在线路径演示，但其生成贡献尚未进入主实验；"
        "后续应在不改主表相对序的前提下，用开启动态源的对照把这一命题补全。",
    ),
}


def patch_table9_note(doc: Document) -> None:
    """表5-1 数字不动；旁注已在段落 588/590 写明不纳入结论。"""
    return


def main() -> None:
    if not THESIS.exists():
        raise SystemExit(f"找不到 {THESIS}")
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup = THESIS.with_name(f"华东师范大学硕士论文_备份_{stamp}.docx")
    shutil.copy2(THESIS, backup)

    doc = Document(str(THESIS))
    paras = doc.paragraphs
    for idx, (prefix, new_text) in REPLACEMENTS.items():
        must_start(paras[idx], prefix)
        set_para_text(paras[idx], new_text)

    # 表4-1 评测协议行：写明主表关动态
    t0 = doc.tables[0]
    if "B0—B3" in t0.rows[6].cells[4].text or "B0-B3" in t0.rows[6].cells[4].text.replace("—", "-"):
        t0.rows[6].cells[4].text = "B0—B3（关动态）+60题问集"

    doc.save(str(THESIS))
    print(f"backup: {backup.name}")
    print(f"patched {len(REPLACEMENTS)} paragraphs + table0 protocol cell")


if __name__ == "__main__":
    main()
