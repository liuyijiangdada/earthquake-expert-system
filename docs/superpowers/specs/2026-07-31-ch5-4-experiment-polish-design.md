# 设计规格：5.4 系统实验与分析理顺

> 日期：2026-07-31  
> 目标文件：`华东师范大学硕士论文.docx`（**定点改写 5.4 区间**）  
> 前置确认：改动范围选 **B**；分阶段结果选 **A**（仅表，不加图5-13）；实现路径选 **定点修补脚本**

---

## 1. 目标与非目标

**目标**

1. 理顺 **5.4 系统实验与分析** 的叙述顺序：设置 → 总表/图 → 分析 → 设计对比 → 消融/分阶段表 → B2 根因 → 案例 → 可用性。
2. 将 **表5-1～表5-4** 改为 `python-docx` **真表格**（不再用 `|` 管道文本）。
3. **重生成并插入** 图5-11、图5-12（中文正常显示；图内标题与正文图号一致为 5-11/5-12）。
4. 正文删除仓库路径（如 `docs/superpowers/architecture/...`）。
5. 压缩 5.4.3 数字堆砌，分阶段明细放入 **表5-4**。

**非目标**

- 不重跑消融实验；主数字仍用 `data/eval/table_6_3_summary_60.json`（B3 事实一致性 82.5% 等）。
- 不虚构用户问卷/可用性分数。
- 不改 5.1–5.3、5.5、第六章（除非交叉引用必须随表号微调，本轮无此需要）。
- 不新增分阶段柱状图（图5-13）。
- 不整章重跑 `merge_thesis_ch5_ch6.py`（避免误伤界面插图）。

---

## 2. 5.4 定稿结构

```
5.4  系统实验与分析
  5.4.1  实验设置
         · 环境、问集（60题）、四基线、离线关闭动态/第三层、评分口径
         · 表5-1  离线消融实验配置快照
  5.4.2  对比实验结果与分析
         · 简要总述（指向表5-2 / 图5-11 / 图5-12）
         · 表5-2  四基线60题自动评分汇总
         · 图5-11 四基线事实一致性对比
         · 图5-12 四基线要点完整性与格式合规对比
         · 通路增益与完整性非单调讨论
         · 表5-3  与 GraphRAG、KnowledGPT 的设计对比
         · 明确未做同设定数值复现
  5.4.3  消融实验
         · 模块贡献（相对 B0）
         · 表5-4  分阶段（震前/震中/震后）自动评分汇总
         · 阶段解读（短）
         · B2 事实分偏低根因（四点，压缩）
  5.4.4  案例分析（定性演示；与消融问集分离）
  5.4.5  系统可用性评测（待开展）
```

---

## 3. 图表编号与数据

| 编号 | 类型 | 内容 | 数据/素材 |
|------|------|------|-----------|
| 表5-1 | Word 表 | 配置快照：KG/RAG/动态/增强/问量/评分 | 正文既有叙述结构化 |
| 表5-2 | Word 表 | B0–B3 三项指标 | `table_6_3_summary_60.json` → `overall` |
| 表5-3 | Word 表 | GraphRAG / KnowledGPT / 本文 四维设计对比 | 现有文字表内容 |
| 表5-4 | Word 表（新增） | 分阶段事实一致性、完整性、格式合规 | `by_phase` |
| 图5-11 | PNG 插入 | 事实一致性柱状图 | 重生成后写入 architecture 目录并插入 docx |
| 图5-12 | PNG 插入 | 完整性 + 格式合规双图 | 同上 |

图文件建议路径（可覆盖旧文件或另存）：

- `docs/superpowers/architecture/fig-5-11-factual-60.png`
- `docs/superpowers/architecture/fig-5-12-completeness-format-60.png`

生成要求：中文字体可渲染（优先 PingFang SC / Heiti SC / Arial Unicode）；图内标题写「图5-11 …」「图5-12 …」；数值与 JSON 一致。

---

## 4. 叙述顺序约束

1. **先表后析**：表5-2 / 图出现后，再写通路增益与完整性讨论。  
2. **表题在表前**：表5-3 标题不得再落在表格正文之后。  
3. **分阶段数字进表**：5.4.3 正文不再罗列全部 Comp/Fmt 分阶段串；用 1–2 句点出趋势并引用表5-4。  
4. **无仓库路径**：插图说明只用图号，不写本地相对路径。  
5. **口径边界不变**：离线消融关闭动态与第三层；案例属定性演示；可用性待开展。

---

## 5. 实现方式

- 新建脚本（建议）：`scripts/polish_thesis_ch5_4.py`  
  1. 备份 `华东师范大学硕士论文.docx.bak-before-ch5-4-polish`  
  2. 定位段落「5.4  系统实验与分析」至「5.5  本章小结」之前  
  3. 删除该区间旧段落（含管道文本表、空图题）  
  4. 按定稿结构插入正文、真表格、图与图题  
  5. 从 JSON 填充数值  
- 图表生成可内嵌于脚本或独立小脚本（matplotlib）。  
- 不修改 `merge_thesis_ch5_ch6.py` 主流程；可选同步更新 `thesis_ch5_merge_content.py` 中 5.4 块以免日后重复合并倒退（**可选，非本轮必须**）。

---

## 6. 验收清单

- [ ] 5.4.1–5.4.5 标题齐全，顺序符合 §2  
- [ ] 表5-1～5-4 均为 Word 表格；表题紧挨表前  
- [ ] 图5-11、图5-12 已插入且中文可读；图号非 6-x  
- [ ] 正文无 `docs/superpowers/` 等路径字符串  
- [ ] 表5-2 / 图数值与 `table_6_3_summary_60.json` 一致（B3 事实 82.5% 等）  
- [ ] 存在表5-4 分阶段汇总；5.4.3 无大段 Comp/Fmt 数字串  
- [ ] 5.1–5.3 界面/架构插图数量与内容未因本脚本被清空  
- [ ] 备份文件存在，可回滚  

---

## 7. 自我审查记录

- Placeholder：无 TBD；图路径与表号已写死。  
- 一致性：实验主口径统一 60 题；与第五章合并规格中的图5-11/5-12 编号对齐（界面图仍为 5-4～5-10）。  
- 范围：仅 5.4 + 必要图文件；不加图5-13。  
- 歧义：表5-4 列设计定为「阶段 × 基线」或「指标分块」——**采用按阶段分行、基线分列的宽表，或「阶段 | 指标 | B0–B3」长表；实现时优先「阶段 × 事实一致性四列」+「阶段 × 完整性」+「阶段 × 格式」三张子表合并为一张多级表头表，若排版过宽则拆为「表5-4a/b」但正文仍称表5-4 并以一块呈现为佳。定稿实现：一张表，列 = 阶段、指标、B0、B1、B2、B3（每阶段三行指标）。**
